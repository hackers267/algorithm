package sort;

import org.junit.Test;

import static org.junit.Assert.assertTrue;

public class DemoTest {
    @Test public void testSome(){
        Demo demo = new Demo();
        assertTrue("some",demo.some());
    }
}
